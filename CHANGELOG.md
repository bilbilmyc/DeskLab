# 变更记录

版本以对应 Git 标签、源码提交和 Release 构建记录为准。

## 1.1.0 · 本机体检与诊断

- 增加自动本机体检：虚拟化 / WHPX、QEMU、UEFI、可用内存、目录写入、磁盘空间和本机端口，并提供处理建议和手动重检。
- 支持导出本地诊断 JSON；仅记录体检结果及本次运行的固定操作状态和错误类别，不收集原始敏感日志。

## 1.0.0 · 正式版本

- 基于 1.0.0-rc1 的功能基线发布 Windows x64 基础版，包含 Bun、网页、托盘、QEMU、固件与安装配方；不含客体 ISO 或独立 Docker Linux 镜像。
- 应用界面、API、安装器、构建清单和 Windows PE 版本统一为 `1.0.0`。
- 第一方源码采用 MIT 许可证；安装包与构建附件附带 LICENSE 和第三方组件说明。
- 固定并校验构建依赖，QEMU 下载增加传输时限、重试和校验后缓存。
- 标签触发构建与发布；附件先上传草稿，核对安装包、程序及 GitHub SHA-256 后公开。正式版可通过 Releases 和 Latest 下载。
- 保留单用户本地测试定位和既有兼容性限制；完整 Docker 组件继续单独验证。

## 1.0.0-rc1 · 候选版本

- 在 0.3.6 已验证功能基线上进入候选分支，不声明正式版发布日期。
- README 与使用文档统一说明 NAT、SSH、ISO 检查、关机确认、数据备份及基础/完整安装包差异。
- 提供 `master` / RC 分支的 GitHub Windows 基础构建、启动检查和带哈希的产物，不自动创建正式 Release。
- Windows PE 使用数字版本 `1.0.0`，应用界面、API 和安装器保留 `1.0.0-rc1`。

## 0.3.6 基线

- 个人 Windows/Linux 虚拟机工作区、模板复用和五种固定 ISO 自动安装。
- 内置/外部 Docker、受限 Compose、NAT、SSH 密钥与端口映射、SQLite 存储。
- 统一界面字体和边框；创建窗口采用紧凑双栏与固定操作区。
- 创建前 ISO 类型与版本检查；未知介质手动确认；错配不留下空实例。
- 普通关机增加二次确认和默认取消，保留强制关闭的独立警告。
- 初始化公开源码仓库，整理用户与开发文档，增加 Windows 基础构建 CI。
