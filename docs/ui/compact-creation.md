# 创建弹窗 · 0.3.4

保持 0.3.3 的全局字体、字重与边框。只调整创建环境弹窗：桌面宽度最多 860px，名称和安装盘位于左侧，CPU、内存和磁盘位于右侧；680px 以下恢复单列。选择系统、自动安装、已有模板和手动 ISO 共用固定标题与底部操作，中间内容独立滚动。

系统说明与重新选择入口合并为一行；网络和登录说明收进可展开区域，默认显示 NAT 和连接方式摘要。资源字段始终在主表单内，保留原有默认值、最低要求和原生必填校验。错误显示在底部操作上方，不会随着表单滚动而消失；文件选择保持独立弹窗与取消状态。

## 验证

- `scripts/checks/create-dialog-ui.mjs`：77 项检查通过，覆盖 1920×1080、1366×768、1024×768、768×600、320×480。自动安装默认内容在前三种尺寸无需滚动，弹窗高度低于 660px；短窗口中滚动内容不移动底部操作。
- 验证空 ISO 阻止提交并聚焦输入框、文件选择和取消、资源与配方提交、NAT 默认、展开 SSH 说明、错误可见、Escape 退出及焦点返回、选择系统和手动 ISO。
- 原有可读性/资源检查 50 项通过，保持 4K / 2K / 1080p 和高 DPI 布局。所有创建请求均在浏览器回归中拦截，不创建用户实例。
- TypeScript 与生产构建通过；打包 EXE 的 39 项隔离服务检查和 416 项浏览器检查通过。报告：`.runtime/checks/desktop-service-CZCNMO/report.json`，弹窗截图：`.runtime/checks/create-dialog/`。

已覆盖安装至 `D:\apps\DeskLab`，版本为 0.3.4，EXE 哈希与构建清单一致。实际安装版在 1366×768 下测得弹窗宽 860px、高约 589px，中间内容高度与可视高度均为 407px，无需滚动。真实截图与测量：`.runtime/checks/create-dialog/installed-1366x768.png`、`installed-report.json`。验证只打开并关闭弹窗，没有创建额外实例。

升级前正常关闭并在升级后恢复 Ubuntu 与 Windows，核对实例/模板 ID、资源配置、QEMU 身份和 Ubuntu SSH 入口；独立 Docker 的数据与停止状态、外部容器及运行状态均保留。10 项升级检查与备份：`.runtime/backups/before-0.3.4-1789223540928/`。本轮临时开发服务和隔离测试服务均已退出。
